# Supabase project settings (Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Postgres connection string used by Drizzle (Project Settings > Database > Connection string)
# Use the "Session pooler" URI (IPv4-compatible, supports prepared statements —
# works for both the app and `drizzle-kit` migrations). The direct connection
# is IPv6-only and unreachable from many networks; see DECISIONS.md.
DATABASE_URL=
